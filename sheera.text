
#include <stdio.h>

int main()
{
    int i,n=1,y=1;
   
    for(i=3;i<=100;i++)
        {
         if(y!=2)   
         printf("%d",i);
         if(i>10)
         printf(" ");
    if(n%13==0)
    {
    printf("\n");
    n++;
    y=1;
    }
        while(i<=9)
        {
        printf("  ");
        break;
        }
       y++;

}
    return 0;
}
